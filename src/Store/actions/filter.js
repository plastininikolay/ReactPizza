export const changeCategory = (category) => (
    {
        type: 'CHANGE_CATEGORY',
        payload: category
    }
)
export const changeSort = (sort) => (
    {
        type: 'CHANGE_SORT',
        payload: sort
    }
)